<%= simple_form_for([shop, shop.remarks.build]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, label: "内容", autofocus: true, required: false %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "评论" %>
  </div>
<% end %>
